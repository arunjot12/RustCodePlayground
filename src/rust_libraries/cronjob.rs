// use cronjob::CronJob;

pub fn cron() {
    println!("Cronjob requires 'cronjob' crate in Cargo.toml.");
    // let mut cron = CronJob::new("My Job", |_name: &str| {
    //     println!("Running scheduled task!");
    // });

    // Runs every 10 seconds
    // cron.seconds("*/5");
    // cron.start_job();
}
